<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Holo">
        
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <item name="android:textViewStyle">@style/AnnPlayerTextViewStyle</item>
        <item name="android:buttonStyle">@style/AnnPlayerButtonStyle</item>
        <item name="android:windowBackground">@color/app_bg_color</item>
    </style>
    
    <style name="AnnPlayerTextViewStyle" parent="android:style/Widget.Holo.TextView">
        <item name="android:textSize">20sp</item>
        <item name="android:padding">10dp</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:drawablePadding">10dp</item>
    </style>
    
    <style name="AnnPlayerButtonStyle" parent="android:style/Widget.Holo.Button">
        <item name="android:textSize">20sp</item>
        <item name="android:padding">10dp</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:drawablePadding">10dp</item>
    </style>

</resources>
